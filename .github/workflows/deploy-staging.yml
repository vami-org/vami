name: deploy-staging

on:
  push:
    branches:
      - staging

jobs:
  ci-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run CI placeholder
        run: echo "Staging pre-deployment checks passed."

  deploy:
    needs: ci-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Deploy Web to Vercel (Staging)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          echo "Deploying to Vercel staging environment..."
          # npx vercel --token=$VERCEL_TOKEN --scope=vami-org --prod
      - name: Deploy API to Railway (Staging)
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          echo "Deploying to Railway staging environment..."
          # npm i -g @railway/cli
          # railway up --service vami-api-staging
